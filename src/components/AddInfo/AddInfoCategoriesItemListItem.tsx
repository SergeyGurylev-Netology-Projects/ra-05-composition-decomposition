/**
 * Элемент из списка элементов из блока прочей информации под рекламой (например, Недвижимость, Маркет, Авто.ру из элемента Посещаемое)
 */

export function AddInfoCategoriesItemListItem({...props}) {

    return (
        <li>
            {props.id}
        </li>
    )
}